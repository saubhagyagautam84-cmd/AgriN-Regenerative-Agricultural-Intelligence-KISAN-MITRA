# Loose pins on purpose: this repo has to install on whatever Python the
# team has (3.10 -> 3.14). Tighten these before any real deployment.
fastapi>=0.115
uvicorn>=0.34
pydantic>=2.9
pandas>=2.2
scikit-learn>=1.5
python-multipart>=0.0.9
# Verifies Firebase Phone Auth ID tokens using only the project ID (fetches
# Google's public certs itself) - no service-account JSON needed. See
# services/auth.py.
google-auth>=2.35
requests>=2.32
# Optional - only needed if TELEPHONY_PROVIDER=twilio (see services/telephony.py).
# The default "console" simulator needs no extra package; install this
# yourself when you actually have a Twilio account to wire in.
# twilio>=9.0
